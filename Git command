
After creating new repository(master):

  push repository

    git init
    git add .
    git commit -m "first commit"
    git remote add origin https://github.com/shailaja-enbake/GitHub.git
    git push -u origin master


Creating new branch "init" :

    git checkout -b init
    git add .
    git commit -m "SIGN IN to LOGIN"
    git push -u origin init


Creating new branch "step2" :

    git checkout -b step2
    git add .
    git commit -m "LOGIN to GET IN"
    git push -u origin step2

    
